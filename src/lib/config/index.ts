// ---------------------------------------------------------------------------
// Symphony Configuration Layer — Public API
// ---------------------------------------------------------------------------

export { DEFAULTS } from './defaults';
export { resolveConfig, validateDispatchConfig } from './resolver';
export type { ValidationResult } from './resolver';
